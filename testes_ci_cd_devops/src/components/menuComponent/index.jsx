import React from 'react';
import { StyledNav } from './styled';

const Index = ({shownav}) => {
    return (

        <StyledNav shownav={false}>

        </StyledNav>
    );
}

export default Index;
